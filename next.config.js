/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // i18n: {
  //   locales: ['fr', 'en-US', 'nl-NL'],
  //   defaultLocale: 'fr',
  // },
  // localesPath: 'fr'
};

module.exports = nextConfig;
